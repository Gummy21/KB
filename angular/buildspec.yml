version: 0.2

phases: 
  install:
    commands:
      - echo Install phase
    finally:
      -echo install finally
  pre_build:
    commands:
      -echo Pre Build
  build:
    commands:
      - echo Build phase
  post_build:
    commands:
      -echo Post Build